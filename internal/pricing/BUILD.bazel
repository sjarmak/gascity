load("@rules_go//go:def.bzl", "go_library", "go_test")

go_library(
    name = "pricing",
    srcs = [
        "build.go",
        "defaults.go",
        "pricing.go",
    ],
    importpath = "github.com/gastownhall/gascity/internal/pricing",
    visibility = ["//:__subpackages__"],
)

go_test(
    name = "pricing_test",
    srcs = [
        "build_test.go",
        "defaults_test.go",
        "pricing_test.go",
        "testenv_import_test.go",
    ],
    embed = [":pricing"],
    deps = ["//internal/testenv"],
)

# --- bazel_repo_srcs (managed by tools/bazel/repo_tree.py) ---

filegroup(
    name = "bazel_repo_srcs",
    srcs = glob(
        ["**"],
        allow_empty = True,
        exclude = ["BUILD.bazel"],
    ),
    visibility = ["//visibility:public"],
)
