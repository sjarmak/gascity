load("@rules_go//go:def.bzl", "go_binary", "go_library")

go_library(
    name = "fakeacp_lib",
    srcs = [
        "agent.go",
        "main.go",
        "options.go",
        "recorder.go",
        "turn.go",
        "wire.go",
    ],
    importpath = "github.com/gastownhall/gascity/internal/runtime/acp/testdata/fakeacp",
    visibility = ["//visibility:private"],
)

go_binary(
    name = "fakeacp",
    embed = [":fakeacp_lib"],
    visibility = ["//:__subpackages__"],
)

# --- bazel_repo_srcs (managed by tools/bazel/repo_tree.py) ---

filegroup(
    name = "bazel_repo_srcs",
    srcs = glob(
        ["**"],
        allow_empty = True,
        exclude = ["BUILD.bazel"],
    ),
    visibility = ["//visibility:public"],
)
