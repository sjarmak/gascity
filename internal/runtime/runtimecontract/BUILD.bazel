load("@rules_go//go:def.bzl", "go_library", "go_test")

go_library(
    name = "runtimecontract",
    srcs = [
        "catalog.go",
        "probes.go",
        "report.go",
        "runner.go",
    ],
    importpath = "github.com/gastownhall/gascity/internal/runtime/runtimecontract",
    visibility = ["//:__subpackages__"],
    deps = ["//internal/runtime"],
)

go_test(
    name = "runtimecontract_test",
    srcs = [
        "lockstep_test.go",
        "runner_test.go",
        "testenv_import_test.go",
    ],
    data = glob(
        ["**"],
        exclude = ["BUILD.bazel"],
    ) + [
        "//:go.mod",
        "//internal/runtime/runtimetest:conformance.go",
    ],
    embed = [":runtimecontract"],
    deps = ["//internal/testenv"],
)

# --- bazel_repo_srcs (managed by tools/bazel/repo_tree.py) ---

filegroup(
    name = "bazel_repo_srcs",
    srcs = glob(
        ["**"],
        allow_empty = True,
        exclude = ["BUILD.bazel"],
    ),
    visibility = ["//visibility:public"],
)
