load("@rules_go//go:def.bzl", "go_library", "go_test")

go_library(
    name = "registry",
    srcs = ["registry.go"],
    importpath = "github.com/gastownhall/gascity/internal/runtime/registry",
    visibility = ["//:__subpackages__"],
    deps = [
        "//internal/config",
        "//internal/runtime",
    ],
)

go_test(
    name = "registry_test",
    srcs = [
        "registry_test.go",
        "testenv_import_test.go",
    ],
    embed = [":registry"],
    deps = [
        "//internal/config",
        "//internal/runtime",
        "//internal/testenv",
    ],
)

# --- bazel_repo_srcs (managed by tools/bazel/repo_tree.py) ---

filegroup(
    name = "bazel_repo_srcs",
    srcs = glob(
        ["**"],
        allow_empty = True,
        exclude = ["BUILD.bazel"],
    ),
    visibility = ["//visibility:public"],
)
