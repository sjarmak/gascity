load("@rules_go//go:def.bzl", "go_library", "go_test")

go_library(
    name = "execenv",
    srcs = ["execenv.go"],
    importpath = "github.com/gastownhall/gascity/internal/execenv",
    visibility = ["//:__subpackages__"],
)

go_test(
    name = "execenv_test",
    srcs = [
        "execenv_test.go",
        "testenv_import_test.go",
    ],
    data = glob(
        ["**"],
        exclude = ["BUILD.bazel"],
    ),
    embed = [":execenv"],
    deps = ["//internal/testenv"],
)

# --- bazel_repo_srcs (managed by tools/bazel/repo_tree.py) ---

filegroup(
    name = "bazel_repo_srcs",
    srcs = glob(
        ["**"],
        allow_empty = True,
        exclude = ["BUILD.bazel"],
    ),
    visibility = ["//visibility:public"],
)
