load("@rules_go//go:def.bzl", "go_library", "go_test")

go_library(
    name = "reliability",
    srcs = [
        "format.go",
        "reliability.go",
    ],
    importpath = "github.com/gastownhall/gascity/internal/reliability",
    visibility = ["//:__subpackages__"],
    deps = ["//internal/events"],
)

go_test(
    name = "reliability_test",
    srcs = [
        "format_test.go",
        "reliability_test.go",
        "testenv_import_test.go",
    ],
    embed = [":reliability"],
    deps = [
        "//internal/events",
        "//internal/testenv",
    ],
)

# --- bazel_repo_srcs (managed by tools/bazel/repo_tree.py) ---

filegroup(
    name = "bazel_repo_srcs",
    srcs = glob(
        ["**"],
        allow_empty = True,
        exclude = ["BUILD.bazel"],
    ),
    visibility = ["//visibility:public"],
)
