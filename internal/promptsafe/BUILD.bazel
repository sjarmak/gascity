load("@rules_go//go:def.bzl", "go_library", "go_test")

go_library(
    name = "promptsafe",
    srcs = ["promptsafe.go"],
    importpath = "github.com/gastownhall/gascity/internal/promptsafe",
    visibility = ["//:__subpackages__"],
)

go_test(
    name = "promptsafe_test",
    srcs = [
        "promptsafe_test.go",
        "testenv_import_test.go",
    ],
    embed = [":promptsafe"],
    deps = ["//internal/testenv"],
)

# --- bazel_repo_srcs (managed by tools/bazel/repo_tree.py) ---

filegroup(
    name = "bazel_repo_srcs",
    srcs = glob(
        ["**"],
        allow_empty = True,
        exclude = ["BUILD.bazel"],
    ),
    visibility = ["//visibility:public"],
)
