load("@rules_go//go:def.bzl", "go_library", "go_test")

go_library(
    name = "citywriteauth",
    srcs = [
        "citywriteauth.go",
        "doc.go",
        "replay.go",
    ],
    importpath = "github.com/gastownhall/gascity/internal/citywriteauth",
    visibility = ["//:__subpackages__"],
)

go_test(
    name = "citywriteauth_test",
    srcs = [
        "citywriteauth_test.go",
        "golden_test.go",
        "testenv_import_test.go",
    ],
    embed = [":citywriteauth"],
    deps = ["//internal/testenv"],
)

# --- bazel_repo_srcs (managed by tools/bazel/repo_tree.py) ---

filegroup(
    name = "bazel_repo_srcs",
    srcs = glob(
        ["**"],
        allow_empty = True,
        exclude = ["BUILD.bazel"],
    ),
    visibility = ["//visibility:public"],
)
