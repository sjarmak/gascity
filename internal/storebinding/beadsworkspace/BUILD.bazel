load("@rules_go//go:def.bzl", "go_library", "go_test")

go_library(
    name = "beadsworkspace",
    srcs = [
        "engine.go",
        "provider.go",
    ],
    importpath = "github.com/gastownhall/gascity/internal/storebinding/beadsworkspace",
    visibility = ["//:__subpackages__"],
    deps = [
        "//internal/beads",
        "//internal/config",
        "//internal/coordclass",
        "//internal/shellquote",
        "//internal/storebinding",
    ],
)

go_test(
    name = "beadsworkspace_test",
    srcs = [
        "provider_test.go",
        "testenv_import_test.go",
    ],
    data = glob(
        ["**"],
        exclude = ["BUILD.bazel"],
    ),
    embed = [":beadsworkspace"],
    deps = [
        "//internal/beads",
        "//internal/config",
        "//internal/coordclass",
        "//internal/shellquote",
        "//internal/storebinding",
        "//internal/testenv",
    ],
)

# --- bazel_repo_srcs (managed by tools/bazel/repo_tree.py) ---

filegroup(
    name = "bazel_repo_srcs",
    srcs = glob(
        ["**"],
        allow_empty = True,
        exclude = ["BUILD.bazel"],
    ),
    visibility = ["//visibility:public"],
)
