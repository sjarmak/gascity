load("@rules_go//go:def.bzl", "go_library", "go_test")

go_library(
    name = "exec",
    srcs = ["exec.go"],
    importpath = "github.com/gastownhall/gascity/internal/events/exec",
    visibility = ["//:__subpackages__"],
    deps = ["//internal/events"],
)

go_test(
    name = "exec_test",
    srcs = [
        "exec_test.go",
        "testenv_import_test.go",
    ],
    embed = [":exec"],
    deps = [
        "//internal/events",
        "//internal/events/eventstest",
        "//internal/testenv",
    ],
)

# --- bazel_repo_srcs (managed by tools/bazel/repo_tree.py) ---

filegroup(
    name = "bazel_repo_srcs",
    srcs = glob(
        ["**"],
        allow_empty = True,
        exclude = ["BUILD.bazel"],
    ),
    visibility = ["//visibility:public"],
)
