# Mayor tick — 2026-07-06 (Sonnet baseline)

Snapshot source: `docs/evals/fable-baselines/inputs/mayor-scenario-beads-2026-07-06.txt` (3,998 lines, ~13 rigs). Treated as the only available evidence; no live polling.

## 1. Top 5 actions, in order

1. **Close out the slack-full token-leak CRIT: push the merged fixes, land the registry re-cut, rotate the tokens.** `gpk-nvxk` (the newest rollup on this thread) states the code fixes are already MERGED (#162+#163) and only two mechanical steps remain: push/merge the registry re-cut (`bd-gpk-gyv4`) and rotate the leaked tokens. This has been sitting as a live credential exposure through multiple escalation beads (`gc-447263`, `gc-447568`, `gc-448319`) that all say the same thing — work is done, only Stephanie's explicit CRIT-confirm and the mechanical push are outstanding. A merged fix with unrotated tokens is still an active exposure. This outranks everything else because it's the only item on the board that is (a) a live security exposure, (b) fully unblocked on the coding side, and (c) has follow-on gaps already found in the fix itself (`gpk-fbd3`: two more log-injection/redaction gaps in the `gpk-la1y` redaction helpers) that need to ride the same push.

2. **Push/merge the `gc-74rxa` control-dispatcher Dolt-port fix.** Root-caused and branch-ready (`gc-454411`): `bd --sandbox` drops `GC_DOLT_PORT`, so the dispatcher resolves Dolt on port 0 and wedges. This single bug is the traceable root cause of a recurring "control-dispatcher not found / mol-focus-review stranded" failure mode that shows up independently across gascity-packs, mem, and EnterpriseBench over multiple weeks (`gc-410233`, `gc-410431`, `gc-411823`, `gc-403163`, `gc-9422` region). It outranks the other infra items because it's the one with a _diagnosed, branch-ready_ fix sitting unmerged — every day it stays unmerged is another day of recurring wedges elsewhere in the queue that get mis-diagnosed one rig at a time instead of fixed once.

3. **Verify and reconcile the codeprobe feature branch: 24 commits marked closed-as-shipped are unreachable from main.** This is `dr-nsmn4w` in the explicit Ready queue (P1). It's above ordinary P1 work because it's a _trust_ problem, not a feature gap: beads say "shipped," git says otherwise. Given how many other self-reported "DONE"/"branch-ready" claims turn out to need correction in this snapshot (see distrust section), this is the one place where the gap has already been named and quantified — closing it stops real work from silently evaporating.

4. **Ship the EB mol-focus-review execute-step no-op fix (`gc-453005`/`gc-453008`).** `EnterpriseBench-pakh` ran 12 consecutive review/execute cycles across 5 different worker sessions, each closing `outcome=pass` with zero diff and zero notes, despite an explicit rejection reason with file:line instructions attached — and it survived an account/capacity swap, ruling out quota throttling as the cause. The fix candidate (reject a "pass" close with no diff and no explicit no-change justification) is scoped and ready to build. This ranks above general backlog work because a step-close gate that accepts empty passes is a silent-failure multiplier: every formula built on `mol-focus-review` is exposed to it, and it directly explains why EB's paper-lock work (`pakh`, grounded-citation gate) has stalled without ever surfacing as blocked.

5. **Dispatch the `mem-cvn3` mol-focus-review dispatch-idempotency guard.** Flagged unrouted for 2+ weeks (`gc-451600`, `gc-451173`) despite recurring costs: triple-dispatch on `ltte.5`, the mi5v 23-latch wedge that's been asked about and never answered, repeated double-dispatch corrections across mem (`mem-5sofv`, `mem-qn3zj`, `mem-3rrl`, `mem-ms4c`). A plan-first bead (`gc-bh3x8`) already exists. This is priority 5, not higher, because unlike items 1–4 it doesn't have a branch-ready fix waiting — it needs to be planned and built — but it's aged longer than any other named infra gap in the snapshot and keeps generating cleanup work in its absence.

## 2. Escalate to Stephanie

- **Slack-full token rotation & registry re-cut (item 1 above):** explicit go/no-go needed — the CRIT-confirm has been logged as "not auto-executing, needs your explicit confirm" three separate times (`gc-447263`, `gc-447568`, `gc-448319`) without resolution. _Decision needed:_ approve rotating the leaked tokens and merging the registry re-cut now.
- **codescalebench PII in public-branch git history** (`co-p8b`, `co-bbs`): aging past 12 days per the rollup chain (`co-8ze`, `co-6c8`, `co-6d4`), described as needing a "recut strategy" pick before next release, with zero autonomous movement possible until she decides. _Decision needed:_ pick the git-history remediation approach (recut vs. other) for the public branch.
- **EnterpriseBench decision trio still open and assigned to her:** `dec-7wp` (ship the sw1w Convoy graceful-degrade fix now vs. hold for repro), `dec-f5g` (wire vs. provision vs. defer the grounded-citation gate that's inert in prod), `dec-xpq` (refactor-orch re-score correction scope + live-spend authorization). _Decision needed:_ one-line rulings on each of the three.
- **codeprobe: comparison-number publication policy.** Rollup `yio9` states she is holding ALL comparison-number publication city-wide for this rig; multiple branch-ready findings (SDLC finding, cap-retune results, 9tk confirm run) are backed up behind it. _Decision needed:_ confirm whether this hold is still in effect, or lift it for specific named findings.
- **gascity-dashboard PR #166** (contributor fix): reviewed twice with `request_changes`; rollups (`gascity-dashboard-jgv8`, `-69nd`) say it's parked awaiting her merge/direction call. _Decision needed:_ merge with requested changes, request the contributor iterate, or close.
- **P0 dolt-version landmine** (`gc-nby7oo`): bundled dolt 2.0.8 corrupts `hq.wisps` and bricks the city on upgrade; the pin to 2.0.4 is filed but I found no evidence it's merged. _Decision needed:_ confirm this pin is authoritative and get it merged before any dolt version bump touches this city.

## 3. Explicitly NOT acting on, despite looking urgent

- **The "ESCALATION: Reaper anomalies detected [MEDIUM]" and "ESCALATION: JSONL spike detected [HIGH]" beads** — not triaging these as N independent incidents. See distrust section: the pattern strongly indicates a spamming alert source, and the correct action is to fix/silence the alerter, not respond to each firing.
- **The historical OOM emergency and enterprisebench/codescalebench rig suspension** (`gc-404606`–`gc-404854`, dated during the mem overnight push): not re-verifying or re-enabling anything here. Later entries in the same rigs (EB k4tv re-run, dashboard publishes) show clear post-incident activity, so this reads as already resolved days before the snapshot date; re-litigating it now would be working stale state.
- **The ~70 near-identical `zeldascension-*` "healthy — quiet tick, rig still fully drained" rollups:** not spending a dispatch cycle "confirming" this rig is fine. It's already over-confirmed — the volume itself is the finding (see below), not a signal that needs action.
- **The hundreds of `gc-XXXXXX` beads that are just Slack thread mirrors** (`slack/T0B17700WUW/...`) and the thousands of `gpk-*`/`gc-*` molecule-scaffolding beads ("Step spec for...", "Finalize workflow", "Load context and inspect the assignment"): these are dispatch plumbing and message echoes, not work items, and treating backlog _size_ as a signal of backlog _severity_ would be a mistake here — the real actionable surface is a small fraction of the ~4,000 lines.
- **The dozens of open exploratory/spec beads under GEO, mem (§4.4 outcome-lift variants), and scix_experiments** (entity-enrichment scale, viz dashboards, section-encoder pilots): these are legitimate backlog but none are blocking anything else and none are aging in a way that compounds — they can wait for a normal-priority dispatch pass, not this tick.

## 4. Signals I distrust, and how that shaped the plan

- **Repeated identical escalation text with no evolving detail.** "ESCALATION: Reaper anomalies detected [MEDIUM]" fires at least 8 times in immediate sequence (`gc-399141` → `gc-400292`) and "ESCALATION: JSONL spike detected [HIGH]" fires at 5 separate points across the whole timeline (`gc-352991`, `gc-398492`, `gc-429081`, `gc-429129`, `gc-452225`) with identical wording each time. Real distinct incidents don't usually repeat verbatim; a broken alert loop does. I did not put these in the top-5 or the escalation list — I'd want the alerting mechanism itself audited before trusting any single firing.
- **Self-reported "DONE" / "branch-ready" / "closed" status from workers, especially under wedge conditions.** The snapshot contains its own explicit counter-evidence: `gc-360993` corrects `gc-360977`'s "done" claim; `gc-451174` reports a duplicate molecule spawned on an already-closed issue; and most concretely, `EnterpriseBench-pakh` closed "pass" 12 times with zero diff. Given that pattern, I did not take any single rig's "branch-ready" or "shipped" rollup line at face value for the priority list — items 1, 2, and 3 above were selected because they have a _second_, corroborating signal (a named root cause, a git-reachability check, or a cross-referenced follow-up bug) rather than resting on the self-report alone.
- **Rollup volume as a health signal.** The zeldascension rig's ~70 "healthy, quiet, fully drained" beads and the EnterpriseBench rig's dozens of tick-rollups are self-generated by the same project-lead loop reporting on itself every cycle. High volume of "all clear" from a single self-reporting source is not stronger evidence than one clear "all clear" — it's the same claim repeated, and the repetition itself (plus the standing, unaddressed bead about bead-store bloat with no retention policy, `gascity-dashboard-essq`: ~247K beads, no purge reaper) told me the noise floor of this city is high enough that low-signal rollups should be discounted, not summed.
- **"Resolved" claims for infra wedges that recur under a different name.** The Dolt-port-zero wedge shows up at least three times under different bead IDs before being root-caused at `gc-74rxa` (`gc-a4v40`, `gc-ie14l`, and the direct RCA). Each intermediate "fixed" or "worked around" note was locally true but didn't hold — which is why item 2 in the priority list is "merge the root-caused fix," not "trust that the workaround already covered it."
