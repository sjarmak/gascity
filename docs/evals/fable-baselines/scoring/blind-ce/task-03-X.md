# Orchestration tick — 2026-07-06 (mayor)

Scope: `/home/ds/gas-city/docs/evals/fable-baselines/inputs/mayor-scenario-beads-2026-07-06.txt` only. No new information assumed. Bead IDs are cited as they appear in the snapshot.

## 1. Actions (5, ranked)

### Rank 1 — Verify and close out the still-active Slack token-leak transport path (gpk-la1y / gpk-zwdb / gpk-fbd3 / gpk-yq22)

**Command-level intent:** Re-run the leak repro that `gpk-yq22` describes ("merged #89 still leaks on transport path") against current gascity-packs `main`, not against the stale fork-main the earlier "fixed" branches were built on. If it still reproduces, direct-instruction-dispatch a fix on top of `gpk-fbd3`'s two already-identified residual gaps (`redactSlackURL` CRLF fallback, `sanitizeSlackErrorBody` U+2028/2029) rather than re-routing through the same formula class implicated in Action 4's verification-gate defect.

**Why it outranks rank 2:** This is external and effectively irreversible — every message that transits the leaking path while this waits exposes a live credential to anyone who can read it, and a leaked Slack token cannot be un-leaked. `apfp`'s scoring corruption (rank 2) is internal and correctable after the fact (bad runs can be re-scored). Irreversible external exposure beats correctable internal corruption.

### Rank 2 — Direct-dispatch the EnterpriseBench-apfp scorer-bug fix, bypassing the formula

**Command-level intent:** `EnterpriseBench-apfp` ("3 NEW on-main silent-misscore bugs") was already pulled out of the broken formula and "handed to mayor for direct dispatch" per `EnterpriseBench-37th`, and is confirmed still pending as of the two most recent EB ticks (`EnterpriseBench-bgr7` ~21:20Z, `EnterpriseBench-tbbr` ~19:00Z, both 2026-07-06: "apfp/mi5v still pending"). Dispatch it now as a direct-instruction session, not a formula sling.

**Why it outranks rank 3:** `apfp` is live, on-main, silent-misscore — every EnterpriseBench scoring run that executes while it waits emits corrupt numbers, and numbers are this rig's entire product. That is active, ongoing data corruption. The control-dispatcher wedge (rank 3) is a stall: it delays work but does not corrupt anything already in flight. Active corruption outranks a stall.

### Rank 3 — Verify deployment (not just existence) of the gc-74rxa control-dispatcher Dolt-port fix

**Command-level intent:** `gc-74rxa` ("control-dispatcher `bd --sandbox` loses GC_DOLT_PORT -> resolves Dolt :0 -> fleet-wide graph.v2 stall") has a fix reported ready at `gc-454411`. But `gc-455573` and `gc-455588` — both later in the mail sequence — still report `EnterpriseBench-mi5v`'s 23-workflow wedge as live and "unanswered." Verify the fix branch is actually merged **and** running on the live sidecars (this city has a recorded failure class for exactly this gap: `gc-107em`, "gcsync installs to GOPATH/bin but nudge-poll sidecars exec stale gc"). If not deployed, drive the deploy step directly.

**Why it outranks rank 4:** This is a shared mechanism wedging multiple rigs at once — EB (`mi5v`, 23 workflows), gascity-packs (`gpk-36gy` control-dispatcher stall), mem dispatch, and dashboard pools all trace to the same Dolt-port defect. A fix that frees a shared dispatcher outranks a locally-scoped verification-gate defect (rank 4), which is already partially mitigated by an interim workaround.

### Rank 4 — Dispatch the EB mol-focus-review zero-diff/false-pass close-gate fix and confirm the bypass is fleet-applied within EB

**Command-level intent:** `gc-453005`/`gc-453008`/`gc-453010` RCA a defect where EB's mol-focus-review EXECUTE step closes `gc.outcome=pass` with zero diff and zero notes — 12 consecutive cycles across 5 worker sessions, confirmed independently via `git log`/`status` each cycle by worker-3, surviving an account/capacity swap. It hit `pakh` and falsely passed `e1eq` four times. Dispatch the proposed fix (execute-step close gate requires either a diff or an explicit no-change justification) to branch-ready, and confirm the interim mitigation ("pakh pulled to direct-instruction dispatch, blocked from formula re-dispatch") is applied to every other EB bead currently mid-formula, not just pakh.

**Why it outranks rank 5:** This defect invalidates an unknown backlog of "done" claims already on record across EB's whole recent history (per the completion-without-artifact rule: repeated zero-diff passes untrust the lane's entire pass history since the first such closure). That is compounding epistemic damage, not a bounded one-off. The Fable P0 item (rank 5) is a real deadline but a single bounded task; an ongoing trust-boundary failure outranks it.

### Rank 5 — Claim/dispatch the ready, no-blocker P0 Fable-distillation baseline-capture work

**Command-level intent:** The ready-queue tail of the snapshot lists `dr-i4v.3` (P0, "Distill cross-repo process skills from Fable articulation runs — TIME-SENSITIVE: Fable authors") and `dr-j0d.1` (P0, "Phase 0: capture Fable golden baselines — 5 planning/orchestration tasks — TIME-SENSITIVE: needs Fable access") as part of "Ready: 20 issues with no active blockers." Both carry an explicit non-recoverable access window in their own titles. Dispatch or claim them this tick.

**Why it outranks the rest of the ready queue:** Per the external-deadline test — "what breaks this tick if it starts an hour later?" — these two are the only ready-with-no-blockers items in the snapshot whose own titles assert a closing access window. Everything else ready-and-unblocked in the tail (`dr-2juyh1`, `dr-1l9hc4`, etc.) is P1 with no stated window and can wait a tick without loss.

## 2. Escalations (3)

1. **Live-spend authorization for pt0n/bvjx (dec-xpq) is built on a doubly-falsified premise.**
   `dec-xpq` ("EB refactor-orch re-score — correction scope + live-spend authorization pt0n/bvjx") is gated on e1eq per `EnterpriseBench-f9vs`. But `EnterpriseBench-fsup`'s "e1eq landed green, pt0n re-runs unblockable" claim was corrected by `EnterpriseBench-p7r9` ("CORRECTION — e1eq topo_order parser fix INCOMPLETE, no-op loop, bvjx precursor still open"), and `gc-453005`'s RCA independently confirms e1eq's execute step falsely passed 4 times with zero diff. **Decision:** authorize the pt0n/bvjx live-spend re-runs now on the current e1eq claim, or hold until e1eq has a fix independently verified by diff (not another mol-focus-review pass)? I will not forward this spend authorization built on a contradicted, RCA-falsified gate — recommend hold.

2. **Rotate the exposed Slack tokens now, or wait for re-verified closure?**
   Credential rotation is a human-scoped, credential-touching action. Given Action 1 treats the leak as still-live (`gpk-yq22`: "merged #89 still leaks on transport path," contradicting `gpk-nvxk`'s "fixes MERGED"), rotation can't wait on my own authority. **Decision:** rotate the affected Slack API/upload tokens immediately (accepting a short reconfiguration window), or hold rotation until Action 1's re-verification confirms the transport path is actually clean? I recommend rotate now — continued exposure compounds every tick it's held.

3. **Codescalebench PII-in-git-history recut strategy is aging past the two-week mark with the rig fully decision-gated.**
   `co-bbs` ("PII still in public-branch git history — pick recut strategy before next release") and `co-p8b` ("Git-history PII remediation for public branch") have been open long enough that rollups (`co-6c8`, `co-6d4`, `co-87w`, `co-8ze`, `co-fjg`, `co-trp`) track the decision's age climbing from ~7d to ~12.6d, with the entire rig "fully decision-gated" on it. **Decision:** pick a recut strategy (rewrite history vs. quarantine-and-forward-fix vs. accept-and-delay-public-release) so codescalebench can resume — this is not something I can pick on your behalf given it touches public-repo history and release timing.

## 3. Deliberate non-actions

- **~30+ "ESCALATION: Reaper anomalies detected [MEDIUM]" and "JSONL spike detected [HIGH]" beads** (`gc-399141` through `gc-453921` and others). Pattern-matched noise: near-identical templated text recurring across dozens of independently-numbered beads with no differentiating incident detail attached to any of them. Paging Stephanie on each would burn the escalation channel for no signal gained.

- **`gpk-13ca` maintainer-review-pass churn.** Documented failure loop: repeated force-claim/hold/re-claim cycles (`gc-411110`, `gc-412011`, `gc-412502`, `gc-412948`, `gc-413009`) on a bead explicitly gated to Stephanie's own review pass over 32 open packs PRs. Re-dispatching or self-triaging it mid-tick re-triggers the same churn the reconciler already keeps hitting; the durable fixes are already filed (`gc-d4s4h`, `gc-tmjoj`) and belong in the ordinary work queue, not an ad-hoc intervention.

- **`dec-7wp`** ("ship sw1w Convoy graceful-degrade fix or hold for repro," status still `open`). Stale emergency/decision: `gascity-dashboard-2803` and `gascity-dashboard-1nvjs` show Stephanie already approved and PR #164 already merged. Re-surfacing this as a pending ask would present resolved work as an open decision.

- **The hundreds of `slack/T0B17700WUW/...#N` mirror beads and bare molecule step-scaffold beads** ("Load context and inspect the assignment," "Set up a worktree and branch," "Finalize workflow," "Step spec for…," `sling-gc-*`). Bulk state: these are workflow plumbing and channel mirrors, not incidents. They route to the ordinary reconciler/janitor sweep; touching them individually mid-tick would be destructive noise, not signal.

- **scix-experiments' fleet-wide idle pool.** Looks urgent on volume alone (dozens of "routine tick" rollups, a rig-wide RAM gate holding back dense-lane and ingest work). But it is explicitly a deliberate, operator-owned hold (`scix_experiments-6tt`: "pool idle is a deliberate safety hold"; `scix_experiments-aq2y`: "dense-lane RAM window now mayor-owned, parked for tonight's lull"), not a bug. It surfaces through the mayor-owned RAM-window schedule already in place, not this tick's action list.

## 4. Distrust register

| Signal class                               | Falsifying evidence                                                                                                                                                                                                  | Consequence                                                                                                                                                                                                                                                                                                                                        |
| ------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Correction-pair                            | `EnterpriseBench-fsup` ("e1eq landed green... pt0n re-runs unblockable") corrected by `EnterpriseBench-p7r9` ("CORRECTION — e1eq topo_order parser fix INCOMPLETE, no-op loop")                                      | Escalation 1 (`dec-xpq`) carries this contradiction explicitly; no action in this tick treats "e1eq green" as true.                                                                                                                                                                                                                                |
| Completion-without-artifact, RCA-confirmed | `gc-453005`: EB mol-focus-review EXECUTE step closed `pass` with zero diff/zero notes for 12 consecutive cycles across 5 sessions, independently confirmed via git log/status each cycle, hit `pakh` and `e1eq` (4x) | Every EB "branch-ready"/"pass"/"done" claim from this formula since the pattern began is untrusted pending independent diff verification. This is what makes Action 4 a standalone rank rather than trusting "handed to mayor" framing at face value, and it's what strengthens Escalation 1 from a single correction to an RCA-confirmed pattern. |
| Rollup-vs-rollup contradiction             | `gpk-nvxk` ("fixes MERGED #162+#163") vs. `gpk-yq22` ("merged #89 still leaks on transport path... ready branches based on stale fork main")                                                                         | Action 1 is written as re-verify-then-act, not act-on-"fixed"; Escalation 2 (token rotation) is framed assuming ongoing exposure, not resolved exposure.                                                                                                                                                                                           |
| Hold-vs-activity, stale decision bead      | `dec-7wp` (status `open`) vs. `gascity-dashboard-2803`/`-1nvjs` (Stephanie approved, PR #164 merged)                                                                                                                 | Dropped from escalations to non-actions; would otherwise re-ask a resolved question.                                                                                                                                                                                                                                                               |
| Self-report / unconfirmed deploy           | `gc-454411` ("polecat fix ready: gc-74rxa...") vs. later mail `gc-455573`/`gc-455588` still reporting the `mi5v` 23-workflow wedge as live                                                                           | Action 3 is written as "verify deployment, then act," not "fix exists, assume it's running" — this city has a recorded failure class for exactly that gap (`gc-107em`).                                                                                                                                                                            |
| Staleness / bulk pattern                   | Dozens of near-identical "Reaper anomalies" / "JSONL spike" escalation beads, no differentiating detail                                                                                                              | Collapsed into one non-action entry (pattern-matched noise) instead of treated as N separate live incidents.                                                                                                                                                                                                                                       |
