{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "gc://schemas/beads/close-exact/result.schema.json",
  "title": "gc beads close-exact result",
  "description": "Result of an identity-checked, revision-fenced exact-store close.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "ok",
    "bead_id",
    "store_ref",
    "surface",
    "outcome"
  ],
  "properties": {
    "schema_version": {
      "description": "Schema version for this result.",
      "const": "1"
    },
    "ok": {
      "description": "True only after identity validation and authoritative close readback.",
      "const": true
    },
    "bead_id": {
      "description": "Exact bead identifier supplied by the caller.",
      "type": "string",
      "minLength": 1,
      "maxLength": 512,
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]*$"
    },
    "store_ref": {
      "description": "Canonical exact local store selected for the operation.",
      "type": "string",
      "maxLength": 260,
      "pattern": "^(city|rig):[A-Za-z0-9][A-Za-z0-9._-]*$"
    },
    "surface": {
      "description": "Provider surface selected within the exact scope.",
      "type": "string",
      "enum": [
        "file"
      ]
    },
    "outcome": {
      "description": "Durable classification of the exact close.",
      "type": "string",
      "enum": [
        "closed",
        "already_closed"
      ]
    }
  }
}
