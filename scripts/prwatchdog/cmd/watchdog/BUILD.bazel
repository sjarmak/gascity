load("@rules_go//go:def.bzl", "go_binary", "go_library")

go_library(
    name = "watchdog_lib",
    srcs = ["main.go"],
    importpath = "github.com/gastownhall/gascity/scripts/prwatchdog/cmd/watchdog",
    visibility = ["//visibility:private"],
    deps = ["//scripts/prwatchdog"],
)

go_binary(
    name = "watchdog",
    embed = [":watchdog_lib"],
    visibility = ["//visibility:public"],
)

# --- bazel_repo_srcs (managed by tools/bazel/repo_tree.py) ---

filegroup(
    name = "bazel_repo_srcs",
    srcs = glob(
        ["**"],
        allow_empty = True,
        exclude = ["BUILD.bazel"],
    ),
    visibility = ["//visibility:public"],
)
